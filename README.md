# nest-defusal
The NEST lab uses Git to defuse a code bomb!

## In order to build and run this program:

`mkdir build`  
`cd build`  
`cmake ../`  
`make`  

## To run the program
`cd ..`  
`./nest-defusal`
